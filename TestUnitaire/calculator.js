const addition = (x,y) => {return x + y}

const soustraction = (x,y) => {return x-y}

const multiplication = (x,y) => {return x*y}

const affiche = () => {return "papaya papayaya"}

module.exports= {
    addition,
    soustraction,
    multiplication,
    affiche
}